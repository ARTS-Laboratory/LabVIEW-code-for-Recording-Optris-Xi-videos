# Record
LabVIEW code to operate Optris thermal cameras.

The software development kit (SDK) must be installed for the code to function.

Implementation of camera preview footage in LabVIEW is in progress. There is no preview. 

To use:
1. Set the server path to `imagerServer.py`.
2. Run the VI.
3. Wait for the terminal to indicate that the thermal data is ready.
4. Click **Connect Client** to enable the recording features.

> **Note:** Before recording, press `flag` for best temperature accuracy. 


